n = int(input())

# Upper half (1 to N)
for i in range(1, n + 1):
    stars = "* " * i
    spaces = "  " * (2 * (n - i))
    print(stars + spaces + stars)

# Lower half (N down to 1)
for i in range(n, 0, -1):
    stars = "* " * i
    spaces = "  " * (2 * (n - i))
    print(stars + spaces + stars)
